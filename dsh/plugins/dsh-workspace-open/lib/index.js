// Host half is intentionally minimal: this plugin only contributes browser UI.
// The client half registers the header button; the host has no services, tools,
// or events to add.
export function apply() {}
